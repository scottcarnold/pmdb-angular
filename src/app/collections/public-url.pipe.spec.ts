import { PublicUrlPipe } from './public-url.pipe';

describe('PublicUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new PublicUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
